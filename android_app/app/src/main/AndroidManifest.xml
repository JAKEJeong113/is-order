<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- required="false"로 둬서 카메라 없는 기기(태블릿 등)에서도 설치는 가능하게 함 -
         스캔 기능만 못 쓰고 텍스트 검색은 그대로 된다. -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />

    <!-- 오더퀸 다중선택 등록(OqRegisterService)용 - 상품 여러 개를 순차
         등록하는 동안 사용자가 홈 화면으로 나가면 안드로이드가 백그라운드
         네트워크를 막아 등록이 통째로 실패하는 문제(실측: DNS 조회 자체가
         실패)가 있었다. 포그라운드 서비스로 돌리면 알림이 떠 있는 동안은
         이 제한을 받지 않는다. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.BarcodeSite"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 네이티브 카메라(CameraX+ML Kit) 바코드 스캐너 - MainActivity가
             웹페이지의 JS 인터페이스(AndroidScanner) 호출로만 띄운다. -->
        <activity
            android:name=".ScanActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.BarcodeSite" />

        <!-- 오더퀸 다중선택 등록 작업 - OrderQueenDialogs.showRegisterMultipleDialog가
             "등록"을 누르면 시작한다. -->
        <service
            android:name=".OqRegisterService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
    </application>
</manifest>
