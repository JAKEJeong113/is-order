# 본체(is-order)와 완전히 같은 Postgres를 가리켜야 한다 - Render 대시보드에서
# is-order 서비스의 DATABASE_URL 값을 그대로 복사해서 쓴다.
DATABASE_URL=

# 결과 하단 "i's ORDER 자세히 보기" 링크가 가리킬 본체 주소. 기본값은
# 프로덕션 도메인이라 로컬 테스트 때만 바꾸면 된다.
MAIN_SITE_URL=https://www.is-cream.co.kr
