# CLAUDE.md

## 배포
- 코드 수정을 마치면 사용자 확인을 기다리지 말고 바로 커밋 후 `main`에 push한다.
  Render가 `main` push를 감지해서 자동으로 배포하는 구조라, push 자체가 배포
  트리거다(별도의 "배포" 명령이 없음).
- 한 번의 요청/작업 단위에서 나온 변경은 여러 커밋으로 쪼개지 말고 하나로
  묶어서 커밋한다.
- 단, `git reset --hard`, `git push --force`, 브랜치 삭제 등 되돌리기 어려운
  작업은 이 사전 승인에 포함되지 않는다 - 그런 작업은 여전히 매번 확인받는다.
